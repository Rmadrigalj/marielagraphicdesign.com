/* Estilos generales */
body {
  font-family: Arial, sans-serif;
  margin: 0;
  padding: 0;
}

header {
  background-color: #f0f0f0;
  padding: 20px;
  text-align: center;
}

nav {
  background-color: #333;
  color: #fff;
  padding: 10px;
  text-align: center;
}

main {
  padding: 20px;
}

.frame {
  display: flex;
  flex-direction: column;
}

.frame-section {
  margin-bottom: 20px;
}

footer {
  background-color: #333;
  color: #fff;
  padding: 10px;
  text-align: center;
}

/* Estilos para la galería de imágenes */
.scroll-menu {
  overflow-x: auto;
  /* Permite el desplazamiento horizontal */
  white-space: nowrap;
  /* Evita el salto de línea entre imágenes */
}

.contenedor-imagenes {
  display: inline-block;
}

.imagen-galeria {
  display: inline-block;
  margin-right: 10px;
  cursor: pointer;
  /* Cambiar el cursor al pasar sobre la imagen */
  transition: transform 0.3s;
  /* Transición suave */
}

.imagen-galeria:hover {
  transform: scale(1.2);
  /* Aumenta el tamaño al pasar el ratón */
}

.imagen-galeria.ampliada {
  transform: scale(1.5);
  /* Cambiar el tamaño de la imagen ampliada */
  z-index: 999;
  /* Asegurar que la imagen ampliada esté por encima de las demás */
}

/* Estilos para el frame de bienvenida */
#bienvenida {
  border: 1px solid silver;
  /* Cambio del color del borde a plata */
  padding: 10px;
  /* Reducir el relleno */
  margin-bottom: 20px;
}

/* Estilos para la sección de contacto */
#contacto {
  border: 1px solid silver;
  /* Cambio del color del borde a plata */
  padding: 10px;
  /* Reducir el relleno */
  text-align: center;
}

#contacto h2 {
  margin-bottom: 10px;
}

#contacto p {
  margin-bottom: 20px;
}

#contacto form {
  display: inline-block;
  text-align: left;
  max-width: 500px;
  margin: 0 auto;
}

.input-group {
  margin-bottom: 10px;
}

.input-group label {
  display: block;
  margin-bottom: 5px;
}

#contacto form input,
#contacto form textarea {
  width: calc(100% - 16px);
  /* Ancho del 100% menos el padding */
  padding: 8px;
  margin-bottom: 10px;
  /* Añade margen inferior para separar los campos */
  border: 1px solid #ccc;
  border-radius: 5px;
  box-sizing: border-box;
  /* Asegura que el padding y el border no afecten el ancho total */
}

.input-group textarea {
  resize: vertical;
}

.button-group {
  text-align: center;
}

.button-group button {
  padding: 10px 20px;
  background-color: #007bff;
  color: #fff;
  border: none;
  border-radius: 5px;
  cursor: pointer;
  transition: background-color 0.9s ease;
}

.button-group button:hover {
  background-color: #0056b3;
}